LEON will use modular service boundaries from the start, even if deployed as one app first.

modular monolith first

microservices later only if needed

Start with clear modules, not separate servers.
